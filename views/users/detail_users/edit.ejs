<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>EXPRESS</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
  </head>
  <body>
    <div class="container mt-5">
    <h2 class="mb-4">Edit Video</h2>
    <form action="/video/update/<%= id %>" method="POST">
        <div class="mb-3">
            <label for="nama" class="form-label">Judul Video</label>
            <input type="text" class="form-control" name="judul_video" value="<%= judul_video %>" required>
        </div>   
        <div class="mb-3">
            <label for="nama" class="form-label">Link Video</label>
            <input type="text" class="form-control" name="link_video" value="<%= link_video %>" required>
        </div>   

        <div class="mb-3">
          <label for="nama" class="form-label">Nama Kelas</label>
          <select class="form-select" aria-label="Pilih Kelas" name="id_kelas" required>
              <option selected>Pilih Kelas</option>
              <% for(var i=0; i < data1.length; i++){ %>
                  <option value="<%= data1[i].id_kelas %>"><%= data1[i].judul %></option>
              <% } %>
          </select>
      </div>
      <input type="hidden" id="userIdInput" name="id_users" value="<%= id_users %>" data-sb-validations="required">
        <div class="mb-3">
        <button type="submit" class="btn btn-sm btn-success">Simpan</button>
        </div>
      </form>
    </div>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
